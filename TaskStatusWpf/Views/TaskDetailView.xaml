<UserControl x:Class="TaskStatusWpf.Views.TaskDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:TaskStatusWpf.Converters">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <conv:TaskStatusToBrushConverter x:Key="TaskStatusToBrushConverter"/>

        <!-- カード影 -->
        <DropShadowEffect x:Key="CardShadow" BlurRadius="18" ShadowDepth="4" Opacity="0.25"/>

        <!-- 入力（ReadOnly表示に使う） -->
        <Style x:Key="ReadOnlyBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <!-- ボタン（メイン/サブ） -->
        <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#93C5FD"/>
                                <Setter Property="Foreground" Value="#F8FAFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ラベル -->
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="2,0,0,6"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#F8FAFC" Padding="20">
        <Border CornerRadius="16"
                Background="White"
                Padding="22"
                Effect="{StaticResource CardShadow}">

            <!-- ✅ オーバーレイを重ねるためGrid -->
            <Grid>
                <!-- Busy中は操作不可 -->
                <Grid IsEnabled="{Binding IsNotBusy}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 見出し -->
                    <Grid Grid.Row="0" Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="タスク詳細" FontSize="22" FontWeight="Bold" Foreground="#111827"/>
                            <TextBlock Text="{Binding TaskId, StringFormat=タスクID：{0}}"
                                       Margin="0,6,0,0"
                                       FontSize="12"
                                       Foreground="#6B7280"/>
                        </StackPanel>

                        <!-- ステータスバッジ -->
                        <Border Grid.Column="1"
                                CornerRadius="10"
                                Padding="10,6"
                                VerticalAlignment="Top"
                                Background="{Binding Task.Status, Converter={StaticResource TaskStatusToBrushConverter}}"
                                Visibility="{Binding Task, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="{Binding Task.Status}"
                                       Foreground="White"
                                       FontWeight="SemiBold"/>
                        </Border>
                    </Grid>

                    <!-- 操作 -->
                    <WrapPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,14">
                        <Button Content="戻る"
                                Command="{Binding BackCommand}"
                                Style="{StaticResource SubButtonStyle}"
                                Margin="0,0,10,0"/>
                        <Button Content="再読み込み"
                                Command="{Binding LoadCommand}"
                                Style="{StaticResource SubButtonStyle}"
                                Margin="0,0,10,0"/>
                    </WrapPanel>

                    <!-- 本文 -->
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- タイトル -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock Text="タイトル" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Style="{StaticResource ReadOnlyBoxStyle}"
                                     Text="{Binding Task.Title}"
                                     MinHeight="40"/>
                        </StackPanel>

                        <!-- ステータス（文字でも表示） -->
                        <StackPanel Grid.Row="1" Margin="0,0,0,12">
                            <TextBlock Text="ステータス" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Style="{StaticResource ReadOnlyBoxStyle}"
                                     Text="{Binding Task.Status}"
                                     MinHeight="40"/>
                        </StackPanel>

                        <!-- 説明 -->
                        <StackPanel Grid.Row="2">
                            <TextBlock Text="説明" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Style="{StaticResource ReadOnlyBoxStyle}"
                                     Text="{Binding Task.Description}"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     Height="260"/>
                        </StackPanel>
                    </Grid>

                    <!-- エラー -->
                    <Border Grid.Row="3"
                            Margin="0,12,0,0"
                            Padding="12"
                            CornerRadius="10"
                            Background="#FEF2F2"
                            BorderBrush="#FCA5A5"
                            BorderThickness="1"
                            Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#B91C1C"
                                   TextWrapping="Wrap"/>
                    </Border>
                </Grid>

                <!-- Busyオーバーレイ -->
                <Border Background="#66FFFFFF"
                        CornerRadius="16"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="220" Height="8"/>
                        <TextBlock Text="読み込み中…"
                                   Margin="0,10,0,0"
                                   Foreground="#6B7280"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Border>
</UserControl>
