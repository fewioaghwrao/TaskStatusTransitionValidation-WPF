<UserControl x:Class="TaskStatusWpf.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:TaskStatusWpf.Converters">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Projects" FontSize="24" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
            <TextBlock Text="{Binding WelcomeText}" VerticalAlignment="Center"/>
            <Button Content="Reload" Command="{Binding LoadCommand}" Margin="12,0,0,0" Width="120"/>

            <!-- Leaderだけ表示したい場合（簡易：IsLeaderをVMに持つ） -->
            <TextBlock Text="  (Leader)" Margin="12,3,0,0" Visibility="{Binding IsLeader, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Projects}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ProjectId" Binding="{Binding ProjectId}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridCheckBoxColumn Header="Archived" Binding="{Binding IsArchived}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Tomato" TextWrapping="Wrap" Margin="0,12,0,0"/>
    </Grid>
</UserControl>
