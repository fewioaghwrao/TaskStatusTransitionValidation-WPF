<UserControl x:Class="TaskStatusWpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Login" FontSize="24" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="Email"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="320"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="Password"/>
            <!-- 最小構成のため PasswordBox 直結は避け、まずは TextBox で -->
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Width="320"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,16,0,0" Orientation="Horizontal">
            <Button Content="Login" Command="{Binding LoginCommand}" Width="120" Height="32"/>
            <TextBlock Text="{Binding IsBusy}" Margin="12,6,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="Tomato" TextWrapping="Wrap" Margin="0,12,0,0"/>
    </Grid>
</UserControl>
