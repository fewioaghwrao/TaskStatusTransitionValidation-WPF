<UserControl x:Class="TaskStatusWpf.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:TaskStatusWpf.Converters">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Background="#1F2937" Padding="12"
            Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">
        <DockPanel LastChildFill="True">
            <TextBlock Text=""
                       Foreground="White"
                       FontSize="16"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Left"/>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <TextBlock Text="{Binding UserText}"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0"/>
                <Button Content="ログアウト"
        Click="LogoutButton_Click"
        Padding="14,0"
        Height="34"
        BorderThickness="1"
        BorderBrush="#374151"
        Background="#111827"
        Foreground="White"/>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
